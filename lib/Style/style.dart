import 'package:flutter/material.dart';

const Color operatorColor = Color(0xff272727);
const Color buttonColor = Color(0xff191919);
const Color orangeColor = Color(0xffD9802E);
const Color colorBlack = Color(0xff000000);
const Color colorWhite = Color(0xffffffff);

TextStyle buttonBodyText(textColor) {
  return TextStyle(
    color: textColor,
    fontSize: 30,
    fontWeight: FontWeight.bold,
  );
}
